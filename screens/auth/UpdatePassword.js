import React from "react";
import { StyleSheet, Text, View } from "react-native";

const UpdatePassword = ({ navigation }) => {
  return (
    <View>
      <Text>Update Password</Text>
    </View>
  );
};

const updatePasswordStyles = StyleSheet.create({});

export default UpdatePassword;
